html(lang="en")
    head
        title= pageTitle
    body
        h1 Créer compte

        form(method='POST' action="") <!-- addition à l'url -->
            p ident
            input#ident(type="text", name="ident")
            p email
            input#email(type="email", name="email")
            p password
            input#password(type="password", name="password")
            p confirm password
            input#confirm_password(type="password", name="confirm_password")
            input#create(type="submit", value="Créer compte", name="create")
            
            //
                button#create(type="submit", value="Créer compte", name="create")
